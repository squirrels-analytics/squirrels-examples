# sqrl-expenses-slim

Minimal working example of a Squirrels project.

Other than the typical files of any Python project (.gitignore, README.md, pyproject.toml, uv.lock, etc.), this project only contains the following additional files:
- seeds/transactions.csv
- squirrels.yml
