[project]
name = "sqrl-expenses"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "squirrels",
]

[tool.uv.sources]
squirrels = { git = "https://github.com/squirrels-analytics/squirrels.git", rev = "main" }
